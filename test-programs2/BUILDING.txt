Build it like so, where local_deb is my install directory analogous to /usr/local:

cmake -DSimbody_ROOT_DIR=/mnt/data/NRP/local_deb -DBUILD_UNVERSIONED_LIBS=True -DSimTK_INSTALL_DIR=/mnt/data/NRP/local_deb -DOPENSIM_INSTALL_DIR=/mnt/data/NRP/local_deb ..
